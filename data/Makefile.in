# Makefile.in generated by automake 1.11.1 from Makefile.am.
# @configure_input@

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004, 2005, 2006, 2007, 2008, 2009  Free Software Foundation,
# Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@

VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkglibexecdir = $(libexecdir)/@PACKAGE@
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = @build@
host_triplet = @host@
@HAVE_LIRC_TRUE@am__append_1 = mex-lircrc
@ENABLE_REBINDER_TRUE@am__append_2 = mex-rebinder-config.desktop
@ENABLE_NETWORKS_TRUE@am__append_3 = mex-networks.desktop
subdir = data
DIST_COMMON = $(am__dist_applications_DATA_DIST) \
	$(am__dist_extra_DATA_DIST) $(am__dist_networks_DATA_DIST) \
	$(am__dist_rebinder_DATA_DIST) $(dist_griloconf_DATA) \
	$(dist_icon_actions_DATA) $(dist_icon_categories_DATA) \
	$(dist_icon_status_DATA) $(dist_infobarstyle_DATA) \
	$(dist_json_DATA) $(dist_shellstyle_DATA) \
	$(dist_slideshowstyle_DATA) $(dist_style_DATA) \
	$(dist_videocontrolsstyle_DATA) \
	$(dist_volumecontrolstyle_DATA) $(srcdir)/Makefile.am \
	$(srcdir)/Makefile.in
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/build/as-compiler-flag.m4 \
	$(top_srcdir)/build/as-linguas.m4 \
	$(top_srcdir)/build/as-plugins.m4 \
	$(top_srcdir)/build/gtk-doc.m4 $(top_srcdir)/build/intltool.m4 \
	$(top_srcdir)/build/introspection.m4 \
	$(top_srcdir)/build/libtool.m4 \
	$(top_srcdir)/build/ltoptions.m4 \
	$(top_srcdir)/build/ltsugar.m4 \
	$(top_srcdir)/build/ltversion.m4 \
	$(top_srcdir)/build/lt~obsolete.m4 $(top_srcdir)/configure.ac
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(SHELL) $(top_srcdir)/build/mkinstalldirs
CONFIG_HEADER = $(top_builddir)/config.h
CONFIG_CLEAN_FILES =
CONFIG_CLEAN_VPATH_FILES =
AM_V_GEN = $(am__v_GEN_$(V))
am__v_GEN_ = $(am__v_GEN_$(AM_DEFAULT_VERBOSITY))
am__v_GEN_0 = @echo "  GEN   " $@;
AM_V_at = $(am__v_at_$(V))
am__v_at_ = $(am__v_at_$(AM_DEFAULT_VERBOSITY))
am__v_at_0 = @
SOURCES =
DIST_SOURCES =
RECURSIVE_TARGETS = all-recursive check-recursive dvi-recursive \
	html-recursive info-recursive install-data-recursive \
	install-dvi-recursive install-exec-recursive \
	install-html-recursive install-info-recursive \
	install-pdf-recursive install-ps-recursive install-recursive \
	installcheck-recursive installdirs-recursive pdf-recursive \
	ps-recursive uninstall-recursive
am__dist_applications_DATA_DIST = mex.desktop mex-shutdown.desktop \
	mex-rebinder-config.desktop mex-networks.desktop
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = f=`echo $$p | sed -e 's|^.*/||'`;
am__install_max = 40
am__nobase_strip_setup = \
  srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*|]/\\\\&/g'`
am__nobase_strip = \
  for p in $$list; do echo "$$p"; done | sed -e "s|$$srcdirstrip/||"
am__nobase_list = $(am__nobase_strip_setup); \
  for p in $$list; do echo "$$p $$p"; done | \
  sed "s| $$srcdirstrip/| |;"' / .*\//!s/ .*/ ./; s,\( .*\)/[^/]*$$,\1,' | \
  $(AWK) 'BEGIN { files["."] = "" } { files[$$2] = files[$$2] " " $$1; \
    if (++n[$$2] == $(am__install_max)) \
      { print $$2, files[$$2]; n[$$2] = 0; files[$$2] = "" } } \
    END { for (dir in files) print dir, files[dir] }'
am__base_list = \
  sed '$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;$$!N;s/\n/ /g' | \
  sed '$$!N;$$!N;$$!N;$$!N;s/\n/ /g'
am__installdirs = "$(DESTDIR)$(applicationsdir)" \
	"$(DESTDIR)$(extradir)" "$(DESTDIR)$(griloconfdir)" \
	"$(DESTDIR)$(icon_actionsdir)" \
	"$(DESTDIR)$(icon_categoriesdir)" \
	"$(DESTDIR)$(icon_statusdir)" "$(DESTDIR)$(infobarstyledir)" \
	"$(DESTDIR)$(jsondir)" "$(DESTDIR)$(networksdir)" \
	"$(DESTDIR)$(rebinderdir)" "$(DESTDIR)$(shellstyledir)" \
	"$(DESTDIR)$(slideshowstyledir)" "$(DESTDIR)$(styledir)" \
	"$(DESTDIR)$(videocontrolsstyledir)" \
	"$(DESTDIR)$(volumecontrolstyledir)"
am__dist_extra_DATA_DIST = folder-tile.png thumb-image.png \
	thumb-video.png mex-lircrc
am__dist_networks_DATA_DIST = networks/background.png \
	networks/content-box.png networks/service-list.png \
	networks/icon-tick.png networks/icon-tick-focus.png \
	networks/icon-info.png networks/icon-lock.png \
	networks/icon-lock-focus.png networks/icon-wifi-0.png \
	networks/icon-wifi-0-focus.png networks/icon-wifi-1.png \
	networks/icon-wifi-1-focus.png networks/icon-wifi-2.png \
	networks/icon-wifi-2-focus.png networks/icon-wifi-3.png \
	networks/icon-wifi-3-focus.png networks/icon-wired.png \
	networks/icon-wired-focus.png networks/mex-networks.css
am__dist_rebinder_DATA_DIST = rebinder/back.json rebinder/home.json \
	rebinder/info.json rebinder/welcome.json rebinder/done.json \
	rebinder/setup.json rebinder/error-back.json \
	rebinder/error-home.json rebinder/error-info.json \
	rebinder/button-bg.png rebinder/button-bg-highlight.png \
	rebinder/frame-bg.png rebinder/icon-back.png \
	rebinder/icon-home.png rebinder/icon-info.png \
	rebinder/remote.png rebinder/remote-back.png \
	rebinder/remote-back-error.png rebinder/remote-back-hl.png \
	rebinder/remote-home.png rebinder/remote-home-error.png \
	rebinder/remote-home-hl.png rebinder/remote-info.png \
	rebinder/remote-info-error.png rebinder/remote-info-hl.png \
	rebinder/style.css
DATA = $(dist_applications_DATA) $(dist_extra_DATA) \
	$(dist_griloconf_DATA) $(dist_icon_actions_DATA) \
	$(dist_icon_categories_DATA) $(dist_icon_status_DATA) \
	$(dist_infobarstyle_DATA) $(dist_json_DATA) \
	$(dist_networks_DATA) $(dist_rebinder_DATA) \
	$(dist_shellstyle_DATA) $(dist_slideshowstyle_DATA) \
	$(dist_style_DATA) $(dist_videocontrolsstyle_DATA) \
	$(dist_volumecontrolstyle_DATA)
RECURSIVE_CLEAN_TARGETS = mostlyclean-recursive clean-recursive	\
  distclean-recursive maintainer-clean-recursive
AM_RECURSIVE_TARGETS = $(RECURSIVE_TARGETS:-recursive=) \
	$(RECURSIVE_CLEAN_TARGETS:-recursive=) tags TAGS ctags CTAGS \
	distdir
ETAGS = etags
CTAGS = ctags
DIST_SUBDIRS = $(SUBDIRS)
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
am__relativize = \
  dir0=`pwd`; \
  sed_first='s,^\([^/]*\)/.*$$,\1,'; \
  sed_rest='s,^[^/]*/*,,'; \
  sed_last='s,^.*/\([^/]*\)$$,\1,'; \
  sed_butlast='s,/*[^/]*$$,,'; \
  while test -n "$$dir1"; do \
    first=`echo "$$dir1" | sed -e "$$sed_first"`; \
    if test "$$first" != "."; then \
      if test "$$first" = ".."; then \
        dir2=`echo "$$dir0" | sed -e "$$sed_last"`/"$$dir2"; \
        dir0=`echo "$$dir0" | sed -e "$$sed_butlast"`; \
      else \
        first2=`echo "$$dir2" | sed -e "$$sed_first"`; \
        if test "$$first2" = "$$first"; then \
          dir2=`echo "$$dir2" | sed -e "$$sed_rest"`; \
        else \
          dir2="../$$dir2"; \
        fi; \
        dir0="$$dir0"/"$$first"; \
      fi; \
    fi; \
    dir1=`echo "$$dir1" | sed -e "$$sed_rest"`; \
  done; \
  reldir="$$dir2"
ACLOCAL = @ACLOCAL@
ALL_LINGUAS = @ALL_LINGUAS@
AMTAR = @AMTAR@
AM_DEFAULT_VERBOSITY = @AM_DEFAULT_VERBOSITY@
AR = @AR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
CATALOGS = @CATALOGS@
CATOBJEXT = @CATOBJEXT@
CC = @CC@
CCDEPMODE = @CCDEPMODE@
CFLAGS = @CFLAGS@
CPP = @CPP@
CPPFLAGS = @CPPFLAGS@
CYGPATH_W = @CYGPATH_W@
DATADIRNAME = @DATADIRNAME@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
DISPLAYCONF_CFLAGS = @DISPLAYCONF_CFLAGS@
DISPLAYCONF_LIBS = @DISPLAYCONF_LIBS@
DSYMUTIL = @DSYMUTIL@
DUMPBIN = @DUMPBIN@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EGREP = @EGREP@
EXEEXT = @EXEEXT@
FGREP = @FGREP@
GCONTROLLER_CFLAGS = @GCONTROLLER_CFLAGS@
GCONTROLLER_LIBS = @GCONTROLLER_LIBS@
GETTEXT_PACKAGE = @GETTEXT_PACKAGE@
GLIB_CFLAGS = @GLIB_CFLAGS@
GLIB_GENMARSHAL = @GLIB_GENMARSHAL@
GLIB_LIBS = @GLIB_LIBS@
GLIB_MKENUMS = @GLIB_MKENUMS@
GMOFILES = @GMOFILES@
GMSGFMT = @GMSGFMT@
GOBJECT_QUERY = @GOBJECT_QUERY@
GREP = @GREP@
GTKDOC_CHECK = @GTKDOC_CHECK@
GTKDOC_MKPDF = @GTKDOC_MKPDF@
GTKDOC_REBASE = @GTKDOC_REBASE@
HTML_DIR = @HTML_DIR@
INSTALL = @INSTALL@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
INSTOBJEXT = @INSTOBJEXT@
INTLLIBS = @INTLLIBS@
INTLTOOL_EXTRACT = @INTLTOOL_EXTRACT@
INTLTOOL_MERGE = @INTLTOOL_MERGE@
INTLTOOL_PERL = @INTLTOOL_PERL@
INTLTOOL_UPDATE = @INTLTOOL_UPDATE@
INTROSPECTION_CFLAGS = @INTROSPECTION_CFLAGS@
INTROSPECTION_COMPILER = @INTROSPECTION_COMPILER@
INTROSPECTION_GENERATE = @INTROSPECTION_GENERATE@
INTROSPECTION_GIRDIR = @INTROSPECTION_GIRDIR@
INTROSPECTION_LIBS = @INTROSPECTION_LIBS@
INTROSPECTION_MAKEFILE = @INTROSPECTION_MAKEFILE@
INTROSPECTION_SCANNER = @INTROSPECTION_SCANNER@
INTROSPECTION_TYPELIBDIR = @INTROSPECTION_TYPELIBDIR@
LD = @LD@
LDFLAGS = @LDFLAGS@
LIBOBJS = @LIBOBJS@
LIBS = @LIBS@
LIBTOOL = @LIBTOOL@
LIPO = @LIPO@
LN_S = @LN_S@
LTLIBOBJS = @LTLIBOBJS@
MAKEINFO = @MAKEINFO@
MEX_API_VERSION = @MEX_API_VERSION@
MEX_CFLAGS = @MEX_CFLAGS@
MEX_DEBUG_CFLAGS = @MEX_DEBUG_CFLAGS@
MEX_LIBS = @MEX_LIBS@
MEX_LIRC_LIBS = @MEX_LIRC_LIBS@
MEX_LT_LDFLAGS = @MEX_LT_LDFLAGS@
MEX_LT_VERSION = @MEX_LT_VERSION@
MEX_MAINTAINER_CFLAGS = @MEX_MAINTAINER_CFLAGS@
MEX_MAJOR_VERSION = @MEX_MAJOR_VERSION@
MEX_MICRO_VERSION = @MEX_MICRO_VERSION@
MEX_MINOR_VERSION = @MEX_MINOR_VERSION@
MEX_PLUGINS_ALL = @MEX_PLUGINS_ALL@
MEX_PLUGINS_SELECTED = @MEX_PLUGINS_SELECTED@
MEX_VERSION = @MEX_VERSION@
MKDIR_P = @MKDIR_P@
MKINSTALLDIRS = @MKINSTALLDIRS@
MSGFMT = @MSGFMT@
MSGFMT_OPTS = @MSGFMT_OPTS@
MSGMERGE = @MSGMERGE@
NETWORKS_CFLAGS = @NETWORKS_CFLAGS@
NETWORKS_LIBS = @NETWORKS_LIBS@
NM = @NM@
NMEDIT = @NMEDIT@
NM_CFLAGS = @NM_CFLAGS@
NM_LIBS = @NM_LIBS@
OBJDUMP = @OBJDUMP@
OBJEXT = @OBJEXT@
OTOOL = @OTOOL@
OTOOL64 = @OTOOL64@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_URL = @PACKAGE_URL@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
PKG_CONFIG = @PKG_CONFIG@
PKG_CONFIG_LIBDIR = @PKG_CONFIG_LIBDIR@
PKG_CONFIG_PATH = @PKG_CONFIG_PATH@
POFILES = @POFILES@
POSUB = @POSUB@
PO_IN_DATADIR_FALSE = @PO_IN_DATADIR_FALSE@
PO_IN_DATADIR_TRUE = @PO_IN_DATADIR_TRUE@
RANLIB = @RANLIB@
REBINDER_CFLAGS = @REBINDER_CFLAGS@
REBINDER_LIBS = @REBINDER_LIBS@
SED = @SED@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
SHELL_CFLAGS = @SHELL_CFLAGS@
SHELL_LIBS = @SHELL_LIBS@
STRIP = @STRIP@
USE_NLS = @USE_NLS@
VERSION = @VERSION@
XGETTEXT = @XGETTEXT@
abs_builddir = @abs_builddir@
abs_srcdir = @abs_srcdir@
abs_top_builddir = @abs_top_builddir@
abs_top_srcdir = @abs_top_srcdir@
ac_ct_CC = @ac_ct_CC@
ac_ct_DUMPBIN = @ac_ct_DUMPBIN@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
am__tar = @am__tar@
am__untar = @am__untar@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
builddir = @builddir@
datadir = @datadir@
datarootdir = @datarootdir@
docdir = @docdir@
dvidir = @dvidir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
htmldir = @htmldir@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localedir = @localedir@
localstatedir = @localstatedir@
mandir = @mandir@
mkdir_p = @mkdir_p@
oldincludedir = @oldincludedir@
pdfdir = @pdfdir@
prefix = @prefix@
program_transform_name = @program_transform_name@
psdir = @psdir@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
srcdir = @srcdir@
sysconfdir = @sysconfdir@
target_alias = @target_alias@
top_build_prefix = @top_build_prefix@
top_builddir = @top_builddir@
top_srcdir = @top_srcdir@
SUBDIRS = appicons
extradir = $(pkgdatadir)/common
dist_extra_DATA = folder-tile.png thumb-image.png thumb-video.png \
	$(NULL) $(am__append_1)
jsondir = $(pkgdatadir)/json
dist_json_DATA = \
	json/media-controls.json	\
	json/slide-show.json		\
	json/info-bar.json		\
	json/info-panel-full.json	\
	json/info-panel-simple.json	\
	$(NULL)

styledir = $(pkgdatadir)/common/style
dist_style_DATA = \
		  style/details-actions-separator.png \
		  style/details-bg.png \
		  style/grid-content-bg.png \
		  style/harderror-bg.png \
		  style/highlight-bg.png \
		  style/item-title-bg.png \
		  style/panel-header-bg.png \
		  style/panel-highlight.png \
		  style/panel-shadow.png \
		  style/queue-spinner.png \
		  style/queue-spinner-highlight.png \
		  style/scroll-bg.png \
		  style/scroll-bar.png \
		  style/background-loop.mkv \
		  style/thumb-noContent.png \
		  style/spinner.png \
		  style.css

shellstyledir = $(pkgdatadir)/shell/style
dist_shellstyle_DATA = \
	shell/style.css		\
	shell/folder-tile-up.png \
	shell/main-bg.png	\
	shell/panel-header-grid-bg.png \
	$(NULL)

videocontrolsstyledir = $(pkgdatadir)/common/style/video-controls
dist_videocontrolsstyle_DATA = \
		style/video-controls/header-vr.png \
		style/video-controls/highlight.png \
		style/video-controls/icon-play-highlight.png \
		style/video-controls/icon-play.png \
		style/video-controls/icon-pause-highlight.png \
		style/video-controls/icon-pause.png \
		style/video-controls/icon-stop-highlight.png \
		style/video-controls/icon-stop.png \
		style/video-controls/main-bg.png \
		style/video-controls/panel-bg.png \
		style/video-controls/timeline-bg.png \
		style/video-controls/timeline-buffer.png \
		style/video-controls/timeline-highlight.png \
		style/video-controls/timeline-highlight-white.png \
		style/video-controls/timeline-playhead.png

slideshowstyledir = $(pkgdatadir)/common/style/slide-show
dist_slideshowstyle_DATA = \
		style/slide-show/highlight-bg-white.png \
		style/slide-show/icon-actualsize-highlight.png \
		style/slide-show/icon-actualsize.png \
		style/slide-show/icon-fullscreen-highlight.png \
		style/slide-show/icon-fullscreen.png \
		style/slide-show/icon-rotate-highlight.png \
		style/slide-show/icon-rotate.png

infobarstyledir = $(pkgdatadir)/common/style/info-bar
dist_infobarstyle_DATA = \
			style/info-bar/graphic-network.png \
			style/info-bar/graphic-rebinder.png

volumecontrolstyledir = $(pkgdatadir)/common/style/volume-control
dist_volumecontrolstyle_DATA = \
			style/volume-control/volume-0.png \
			style/volume-control/volume-1.png \
			style/volume-control/volume-2.png \
			style/volume-control/volume-3.png \
			style/volume-control/volume-4.png \
			style/volume-control/volume-5.png \
			style/volume-control/volume-6.png \
			style/volume-control/volume-7.png \
			style/volume-control/volume-8.png \
			style/volume-control/volume-9.png \
			style/volume-control/volume-10.png

icondir = $(datadir)/icons/hicolor/scalable
icon_statusdir = $(icondir)/status
dist_icon_status_DATA = \
			icons/icon-notifications.png

icon_actionsdir = $(icondir)/actions
dist_icon_actions_DATA = \
			 icons/icon-power.png \
			 icons/icon-power-highlight.png \
			 icons/icon-settings.png \
			 icons/icon-settings-highlight.png \
			 icons/icon-checkbox-off.png \
			 icons/icon-checkbox-off-highlight.png \
			 icons/icon-checkbox-on.png \
			 icons/icon-checkbox-on-highlight.png \
			 icons/icon-info-highlight.png \
			 icons/media-addtoqueue-mex.png \
			 icons/media-addtoqueue-mex-highlight.png \
			 icons/media-removefromqueue-mex.png \
			 icons/media-removefromqueue-mex-highlight.png \
			 icons/media-watch-mex.png \
			 icons/media-watch-mex-highlight.png \
			 icons/media-watch-from-beginning-mex.png \
			 icons/media-watch-from-beginning-mex-highlight.png \
			 icons/x-mex-app-launch-mex.png \
			 icons/x-mex-app-launch-mex-highlight.png \
			 icons/stock_left-mex-highlight.png \
			 icons/stock_left-mex.png \
			 icons/stock_right-mex-highlight.png \
			 icons/stock_right-mex.png

icon_categoriesdir = $(icondir)/categories
dist_icon_categories_DATA = \
			    icons/icon-panelheader-apps.png \
			    icons/icon-panelheader-apps-highlight.png \
			    icons/icon-panelheader-computer-highlight.png \
			    icons/icon-panelheader-computer.png \
			    icons/icon-panelheader-photos.png \
			    icons/icon-panelheader-photos-highlight.png \
			    icons/icon-panelheader-recommended.png \
			    icons/icon-panelheader-recommended-highlight.png \
			    icons/icon-panelheader-recorded.png \
			    icons/icon-panelheader-recorded-highlight.png \
			    icons/icon-panelheader-search.png \
			    icons/icon-panelheader-search-highlight.png \
			    icons/icon-panelheader-tv.png \
			    icons/icon-panelheader-tv-highlight.png \
			    icons/icon-panelheader-queue-highlight.png \
			    icons/icon-panelheader-queue.png \
			    icons/icon-panelheader-usb-highlight.png \
			    icons/icon-panelheader-usb.png \
			    icons/icon-panelheader-videos.png \
			    icons/icon-panelheader-videos-highlight.png

@ENABLE_REBINDER_TRUE@rebinderdir = $(pkgdatadir)/rebinder
@ENABLE_REBINDER_TRUE@dist_rebinder_DATA = \
@ENABLE_REBINDER_TRUE@	rebinder/back.json			\
@ENABLE_REBINDER_TRUE@	rebinder/home.json			\
@ENABLE_REBINDER_TRUE@	rebinder/info.json			\
@ENABLE_REBINDER_TRUE@	rebinder/welcome.json			\
@ENABLE_REBINDER_TRUE@	rebinder/done.json			\
@ENABLE_REBINDER_TRUE@	rebinder/setup.json			\
@ENABLE_REBINDER_TRUE@	rebinder/error-back.json		\
@ENABLE_REBINDER_TRUE@	rebinder/error-home.json		\
@ENABLE_REBINDER_TRUE@	rebinder/error-info.json		\
@ENABLE_REBINDER_TRUE@	rebinder/button-bg.png			\
@ENABLE_REBINDER_TRUE@	rebinder/button-bg-highlight.png	\
@ENABLE_REBINDER_TRUE@	rebinder/frame-bg.png			\
@ENABLE_REBINDER_TRUE@	rebinder/icon-back.png			\
@ENABLE_REBINDER_TRUE@	rebinder/icon-home.png			\
@ENABLE_REBINDER_TRUE@	rebinder/icon-info.png			\
@ENABLE_REBINDER_TRUE@	rebinder/remote.png			\
@ENABLE_REBINDER_TRUE@	rebinder/remote-back.png		\
@ENABLE_REBINDER_TRUE@	rebinder/remote-back-error.png		\
@ENABLE_REBINDER_TRUE@	rebinder/remote-back-hl.png		\
@ENABLE_REBINDER_TRUE@	rebinder/remote-home.png		\
@ENABLE_REBINDER_TRUE@	rebinder/remote-home-error.png		\
@ENABLE_REBINDER_TRUE@	rebinder/remote-home-hl.png		\
@ENABLE_REBINDER_TRUE@	rebinder/remote-info.png		\
@ENABLE_REBINDER_TRUE@	rebinder/remote-info-error.png		\
@ENABLE_REBINDER_TRUE@	rebinder/remote-info-hl.png		\
@ENABLE_REBINDER_TRUE@	rebinder/style.css			\
@ENABLE_REBINDER_TRUE@	$(NULL)

@ENABLE_NETWORKS_TRUE@networksdir = $(pkgdatadir)/networks
@ENABLE_NETWORKS_TRUE@dist_networks_DATA = \
@ENABLE_NETWORKS_TRUE@	networks/background.png			\
@ENABLE_NETWORKS_TRUE@	networks/content-box.png		\
@ENABLE_NETWORKS_TRUE@	networks/service-list.png		\
@ENABLE_NETWORKS_TRUE@	networks/icon-tick.png			\
@ENABLE_NETWORKS_TRUE@	networks/icon-tick-focus.png		\
@ENABLE_NETWORKS_TRUE@	networks/icon-info.png			\
@ENABLE_NETWORKS_TRUE@	networks/icon-lock.png			\
@ENABLE_NETWORKS_TRUE@	networks/icon-lock-focus.png		\
@ENABLE_NETWORKS_TRUE@	networks/icon-wifi-0.png		\
@ENABLE_NETWORKS_TRUE@	networks/icon-wifi-0-focus.png		\
@ENABLE_NETWORKS_TRUE@	networks/icon-wifi-1.png		\
@ENABLE_NETWORKS_TRUE@	networks/icon-wifi-1-focus.png		\
@ENABLE_NETWORKS_TRUE@	networks/icon-wifi-2.png		\
@ENABLE_NETWORKS_TRUE@	networks/icon-wifi-2-focus.png		\
@ENABLE_NETWORKS_TRUE@	networks/icon-wifi-3.png		\
@ENABLE_NETWORKS_TRUE@	networks/icon-wifi-3-focus.png		\
@ENABLE_NETWORKS_TRUE@	networks/icon-wired.png			\
@ENABLE_NETWORKS_TRUE@	networks/icon-wired-focus.png		\
@ENABLE_NETWORKS_TRUE@	networks/mex-networks.css		\
@ENABLE_NETWORKS_TRUE@	$(NULL)

applicationsdir = $(datadir)/applications
dist_applications_DATA = mex.desktop mex-shutdown.desktop $(NULL) \
	$(am__append_2) $(am__append_3)
griloconfdir = $(sysconfdir)/$(PACKAGE)
dist_griloconf_DATA = grilo-system.conf
all: all-recursive

.SUFFIXES:
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      ( cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh ) \
	        && { if test -f $@; then exit 0; else break; fi; }; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --foreign data/Makefile'; \
	$(am__cd) $(top_srcdir) && \
	  $(AUTOMAKE) --foreign data/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(am__aclocal_m4_deps):

mostlyclean-libtool:
	-rm -f *.lo

clean-libtool:
	-rm -rf .libs _libs
install-dist_applicationsDATA: $(dist_applications_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(applicationsdir)" || $(MKDIR_P) "$(DESTDIR)$(applicationsdir)"
	@list='$(dist_applications_DATA)'; test -n "$(applicationsdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(applicationsdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(applicationsdir)" || exit $$?; \
	done

uninstall-dist_applicationsDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_applications_DATA)'; test -n "$(applicationsdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(applicationsdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(applicationsdir)" && rm -f $$files
install-dist_extraDATA: $(dist_extra_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(extradir)" || $(MKDIR_P) "$(DESTDIR)$(extradir)"
	@list='$(dist_extra_DATA)'; test -n "$(extradir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(extradir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(extradir)" || exit $$?; \
	done

uninstall-dist_extraDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_extra_DATA)'; test -n "$(extradir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(extradir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(extradir)" && rm -f $$files
install-dist_griloconfDATA: $(dist_griloconf_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(griloconfdir)" || $(MKDIR_P) "$(DESTDIR)$(griloconfdir)"
	@list='$(dist_griloconf_DATA)'; test -n "$(griloconfdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(griloconfdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(griloconfdir)" || exit $$?; \
	done

uninstall-dist_griloconfDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_griloconf_DATA)'; test -n "$(griloconfdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(griloconfdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(griloconfdir)" && rm -f $$files
install-dist_icon_actionsDATA: $(dist_icon_actions_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(icon_actionsdir)" || $(MKDIR_P) "$(DESTDIR)$(icon_actionsdir)"
	@list='$(dist_icon_actions_DATA)'; test -n "$(icon_actionsdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(icon_actionsdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(icon_actionsdir)" || exit $$?; \
	done

uninstall-dist_icon_actionsDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_icon_actions_DATA)'; test -n "$(icon_actionsdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(icon_actionsdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(icon_actionsdir)" && rm -f $$files
install-dist_icon_categoriesDATA: $(dist_icon_categories_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(icon_categoriesdir)" || $(MKDIR_P) "$(DESTDIR)$(icon_categoriesdir)"
	@list='$(dist_icon_categories_DATA)'; test -n "$(icon_categoriesdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(icon_categoriesdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(icon_categoriesdir)" || exit $$?; \
	done

uninstall-dist_icon_categoriesDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_icon_categories_DATA)'; test -n "$(icon_categoriesdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(icon_categoriesdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(icon_categoriesdir)" && rm -f $$files
install-dist_icon_statusDATA: $(dist_icon_status_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(icon_statusdir)" || $(MKDIR_P) "$(DESTDIR)$(icon_statusdir)"
	@list='$(dist_icon_status_DATA)'; test -n "$(icon_statusdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(icon_statusdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(icon_statusdir)" || exit $$?; \
	done

uninstall-dist_icon_statusDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_icon_status_DATA)'; test -n "$(icon_statusdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(icon_statusdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(icon_statusdir)" && rm -f $$files
install-dist_infobarstyleDATA: $(dist_infobarstyle_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(infobarstyledir)" || $(MKDIR_P) "$(DESTDIR)$(infobarstyledir)"
	@list='$(dist_infobarstyle_DATA)'; test -n "$(infobarstyledir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(infobarstyledir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(infobarstyledir)" || exit $$?; \
	done

uninstall-dist_infobarstyleDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_infobarstyle_DATA)'; test -n "$(infobarstyledir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(infobarstyledir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(infobarstyledir)" && rm -f $$files
install-dist_jsonDATA: $(dist_json_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(jsondir)" || $(MKDIR_P) "$(DESTDIR)$(jsondir)"
	@list='$(dist_json_DATA)'; test -n "$(jsondir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(jsondir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(jsondir)" || exit $$?; \
	done

uninstall-dist_jsonDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_json_DATA)'; test -n "$(jsondir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(jsondir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(jsondir)" && rm -f $$files
install-dist_networksDATA: $(dist_networks_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(networksdir)" || $(MKDIR_P) "$(DESTDIR)$(networksdir)"
	@list='$(dist_networks_DATA)'; test -n "$(networksdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(networksdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(networksdir)" || exit $$?; \
	done

uninstall-dist_networksDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_networks_DATA)'; test -n "$(networksdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(networksdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(networksdir)" && rm -f $$files
install-dist_rebinderDATA: $(dist_rebinder_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(rebinderdir)" || $(MKDIR_P) "$(DESTDIR)$(rebinderdir)"
	@list='$(dist_rebinder_DATA)'; test -n "$(rebinderdir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(rebinderdir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(rebinderdir)" || exit $$?; \
	done

uninstall-dist_rebinderDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_rebinder_DATA)'; test -n "$(rebinderdir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(rebinderdir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(rebinderdir)" && rm -f $$files
install-dist_shellstyleDATA: $(dist_shellstyle_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(shellstyledir)" || $(MKDIR_P) "$(DESTDIR)$(shellstyledir)"
	@list='$(dist_shellstyle_DATA)'; test -n "$(shellstyledir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(shellstyledir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(shellstyledir)" || exit $$?; \
	done

uninstall-dist_shellstyleDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_shellstyle_DATA)'; test -n "$(shellstyledir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(shellstyledir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(shellstyledir)" && rm -f $$files
install-dist_slideshowstyleDATA: $(dist_slideshowstyle_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(slideshowstyledir)" || $(MKDIR_P) "$(DESTDIR)$(slideshowstyledir)"
	@list='$(dist_slideshowstyle_DATA)'; test -n "$(slideshowstyledir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(slideshowstyledir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(slideshowstyledir)" || exit $$?; \
	done

uninstall-dist_slideshowstyleDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_slideshowstyle_DATA)'; test -n "$(slideshowstyledir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(slideshowstyledir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(slideshowstyledir)" && rm -f $$files
install-dist_styleDATA: $(dist_style_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(styledir)" || $(MKDIR_P) "$(DESTDIR)$(styledir)"
	@list='$(dist_style_DATA)'; test -n "$(styledir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(styledir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(styledir)" || exit $$?; \
	done

uninstall-dist_styleDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_style_DATA)'; test -n "$(styledir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(styledir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(styledir)" && rm -f $$files
install-dist_videocontrolsstyleDATA: $(dist_videocontrolsstyle_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(videocontrolsstyledir)" || $(MKDIR_P) "$(DESTDIR)$(videocontrolsstyledir)"
	@list='$(dist_videocontrolsstyle_DATA)'; test -n "$(videocontrolsstyledir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(videocontrolsstyledir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(videocontrolsstyledir)" || exit $$?; \
	done

uninstall-dist_videocontrolsstyleDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_videocontrolsstyle_DATA)'; test -n "$(videocontrolsstyledir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(videocontrolsstyledir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(videocontrolsstyledir)" && rm -f $$files
install-dist_volumecontrolstyleDATA: $(dist_volumecontrolstyle_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(volumecontrolstyledir)" || $(MKDIR_P) "$(DESTDIR)$(volumecontrolstyledir)"
	@list='$(dist_volumecontrolstyle_DATA)'; test -n "$(volumecontrolstyledir)" || list=; \
	for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  echo "$$d$$p"; \
	done | $(am__base_list) | \
	while read files; do \
	  echo " $(INSTALL_DATA) $$files '$(DESTDIR)$(volumecontrolstyledir)'"; \
	  $(INSTALL_DATA) $$files "$(DESTDIR)$(volumecontrolstyledir)" || exit $$?; \
	done

uninstall-dist_volumecontrolstyleDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(dist_volumecontrolstyle_DATA)'; test -n "$(volumecontrolstyledir)" || list=; \
	files=`for p in $$list; do echo $$p; done | sed -e 's|^.*/||'`; \
	test -n "$$files" || exit 0; \
	echo " ( cd '$(DESTDIR)$(volumecontrolstyledir)' && rm -f" $$files ")"; \
	cd "$(DESTDIR)$(volumecontrolstyledir)" && rm -f $$files

# This directory's subdirectories are mostly independent; you can cd
# into them and run `make' without going through this Makefile.
# To change the values of `make' variables: instead of editing Makefiles,
# (1) if the variable is set in `config.status', edit `config.status'
#     (which will cause the Makefiles to be regenerated when you run `make');
# (2) otherwise, pass the desired values on the `make' command line.
$(RECURSIVE_TARGETS):
	@fail= failcom='exit 1'; \
	for f in x $$MAKEFLAGS; do \
	  case $$f in \
	    *=* | --[!k]*);; \
	    *k*) failcom='fail=yes';; \
	  esac; \
	done; \
	dot_seen=no; \
	target=`echo $@ | sed s/-recursive//`; \
	list='$(SUBDIRS)'; for subdir in $$list; do \
	  echo "Making $$target in $$subdir"; \
	  if test "$$subdir" = "."; then \
	    dot_seen=yes; \
	    local_target="$$target-am"; \
	  else \
	    local_target="$$target"; \
	  fi; \
	  ($(am__cd) $$subdir && $(MAKE) $(AM_MAKEFLAGS) $$local_target) \
	  || eval $$failcom; \
	done; \
	if test "$$dot_seen" = "no"; then \
	  $(MAKE) $(AM_MAKEFLAGS) "$$target-am" || exit 1; \
	fi; test -z "$$fail"

$(RECURSIVE_CLEAN_TARGETS):
	@fail= failcom='exit 1'; \
	for f in x $$MAKEFLAGS; do \
	  case $$f in \
	    *=* | --[!k]*);; \
	    *k*) failcom='fail=yes';; \
	  esac; \
	done; \
	dot_seen=no; \
	case "$@" in \
	  distclean-* | maintainer-clean-*) list='$(DIST_SUBDIRS)' ;; \
	  *) list='$(SUBDIRS)' ;; \
	esac; \
	rev=''; for subdir in $$list; do \
	  if test "$$subdir" = "."; then :; else \
	    rev="$$subdir $$rev"; \
	  fi; \
	done; \
	rev="$$rev ."; \
	target=`echo $@ | sed s/-recursive//`; \
	for subdir in $$rev; do \
	  echo "Making $$target in $$subdir"; \
	  if test "$$subdir" = "."; then \
	    local_target="$$target-am"; \
	  else \
	    local_target="$$target"; \
	  fi; \
	  ($(am__cd) $$subdir && $(MAKE) $(AM_MAKEFLAGS) $$local_target) \
	  || eval $$failcom; \
	done && test -z "$$fail"
tags-recursive:
	list='$(SUBDIRS)'; for subdir in $$list; do \
	  test "$$subdir" = . || ($(am__cd) $$subdir && $(MAKE) $(AM_MAKEFLAGS) tags); \
	done
ctags-recursive:
	list='$(SUBDIRS)'; for subdir in $$list; do \
	  test "$$subdir" = . || ($(am__cd) $$subdir && $(MAKE) $(AM_MAKEFLAGS) ctags); \
	done

ID: $(HEADERS) $(SOURCES) $(LISP) $(TAGS_FILES)
	list='$(SOURCES) $(HEADERS) $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '{ files[$$0] = 1; nonempty = 1; } \
	      END { if (nonempty) { for (i in files) print i; }; }'`; \
	mkid -fID $$unique
tags: TAGS

TAGS: tags-recursive $(HEADERS) $(SOURCES)  $(TAGS_DEPENDENCIES) \
		$(TAGS_FILES) $(LISP)
	set x; \
	here=`pwd`; \
	if ($(ETAGS) --etags-include --version) >/dev/null 2>&1; then \
	  include_option=--etags-include; \
	  empty_fix=.; \
	else \
	  include_option=--include; \
	  empty_fix=; \
	fi; \
	list='$(SUBDIRS)'; for subdir in $$list; do \
	  if test "$$subdir" = .; then :; else \
	    test ! -f $$subdir/TAGS || \
	      set "$$@" "$$include_option=$$here/$$subdir/TAGS"; \
	  fi; \
	done; \
	list='$(SOURCES) $(HEADERS)  $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '{ files[$$0] = 1; nonempty = 1; } \
	      END { if (nonempty) { for (i in files) print i; }; }'`; \
	shift; \
	if test -z "$(ETAGS_ARGS)$$*$$unique"; then :; else \
	  test -n "$$unique" || unique=$$empty_fix; \
	  if test $$# -gt 0; then \
	    $(ETAGS) $(ETAGSFLAGS) $(AM_ETAGSFLAGS) $(ETAGS_ARGS) \
	      "$$@" $$unique; \
	  else \
	    $(ETAGS) $(ETAGSFLAGS) $(AM_ETAGSFLAGS) $(ETAGS_ARGS) \
	      $$unique; \
	  fi; \
	fi
ctags: CTAGS
CTAGS: ctags-recursive $(HEADERS) $(SOURCES)  $(TAGS_DEPENDENCIES) \
		$(TAGS_FILES) $(LISP)
	list='$(SOURCES) $(HEADERS)  $(LISP) $(TAGS_FILES)'; \
	unique=`for i in $$list; do \
	    if test -f "$$i"; then echo $$i; else echo $(srcdir)/$$i; fi; \
	  done | \
	  $(AWK) '{ files[$$0] = 1; nonempty = 1; } \
	      END { if (nonempty) { for (i in files) print i; }; }'`; \
	test -z "$(CTAGS_ARGS)$$unique" \
	  || $(CTAGS) $(CTAGSFLAGS) $(AM_CTAGSFLAGS) $(CTAGS_ARGS) \
	     $$unique

GTAGS:
	here=`$(am__cd) $(top_builddir) && pwd` \
	  && $(am__cd) $(top_srcdir) \
	  && gtags -i $(GTAGS_ARGS) "$$here"

distclean-tags:
	-rm -f TAGS ID GTAGS GRTAGS GSYMS GPATH tags

distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's/[].[^$$\\*]/\\\\&/g'`; \
	list='$(DISTFILES)'; \
	  dist_files=`for file in $$list; do echo $$file; done | \
	  sed -e "s|^$$srcdirstrip/||;t" \
	      -e "s|^$$topsrcdirstrip/|$(top_builddir)/|;t"`; \
	case $$dist_files in \
	  */*) $(MKDIR_P) `echo "$$dist_files" | \
			   sed '/\//!d;s|^|$(distdir)/|;s,/[^/]*$$,,' | \
			   sort -u` ;; \
	esac; \
	for file in $$dist_files; do \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  if test -d $$d/$$file; then \
	    dir=`echo "/$$file" | sed -e 's,/[^/]*$$,,'`; \
	    if test -d "$(distdir)/$$file"; then \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -fpR $(srcdir)/$$file "$(distdir)$$dir" || exit 1; \
	      find "$(distdir)/$$file" -type d ! -perm -700 -exec chmod u+rwx {} \;; \
	    fi; \
	    cp -fpR $$d/$$file "$(distdir)$$dir" || exit 1; \
	  else \
	    test -f "$(distdir)/$$file" \
	    || cp -p $$d/$$file "$(distdir)/$$file" \
	    || exit 1; \
	  fi; \
	done
	@list='$(DIST_SUBDIRS)'; for subdir in $$list; do \
	  if test "$$subdir" = .; then :; else \
	    test -d "$(distdir)/$$subdir" \
	    || $(MKDIR_P) "$(distdir)/$$subdir" \
	    || exit 1; \
	  fi; \
	done
	@list='$(DIST_SUBDIRS)'; for subdir in $$list; do \
	  if test "$$subdir" = .; then :; else \
	    dir1=$$subdir; dir2="$(distdir)/$$subdir"; \
	    $(am__relativize); \
	    new_distdir=$$reldir; \
	    dir1=$$subdir; dir2="$(top_distdir)"; \
	    $(am__relativize); \
	    new_top_distdir=$$reldir; \
	    echo " (cd $$subdir && $(MAKE) $(AM_MAKEFLAGS) top_distdir="$$new_top_distdir" distdir="$$new_distdir" \\"; \
	    echo "     am__remove_distdir=: am__skip_length_check=: am__skip_mode_fix=: distdir)"; \
	    ($(am__cd) $$subdir && \
	      $(MAKE) $(AM_MAKEFLAGS) \
	        top_distdir="$$new_top_distdir" \
	        distdir="$$new_distdir" \
		am__remove_distdir=: \
		am__skip_length_check=: \
		am__skip_mode_fix=: \
	        distdir) \
	      || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-recursive
all-am: Makefile $(DATA)
installdirs: installdirs-recursive
installdirs-am:
	for dir in "$(DESTDIR)$(applicationsdir)" "$(DESTDIR)$(extradir)" "$(DESTDIR)$(griloconfdir)" "$(DESTDIR)$(icon_actionsdir)" "$(DESTDIR)$(icon_categoriesdir)" "$(DESTDIR)$(icon_statusdir)" "$(DESTDIR)$(infobarstyledir)" "$(DESTDIR)$(jsondir)" "$(DESTDIR)$(networksdir)" "$(DESTDIR)$(rebinderdir)" "$(DESTDIR)$(shellstyledir)" "$(DESTDIR)$(slideshowstyledir)" "$(DESTDIR)$(styledir)" "$(DESTDIR)$(videocontrolsstyledir)" "$(DESTDIR)$(volumecontrolstyledir)"; do \
	  test -z "$$dir" || $(MKDIR_P) "$$dir"; \
	done
install: install-recursive
install-exec: install-exec-recursive
install-data: install-data-recursive
uninstall: uninstall-recursive

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-recursive
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)
	-test . = "$(srcdir)" || test -z "$(CONFIG_CLEAN_VPATH_FILES)" || rm -f $(CONFIG_CLEAN_VPATH_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-recursive

clean-am: clean-generic clean-libtool mostlyclean-am

distclean: distclean-recursive
	-rm -f Makefile
distclean-am: clean-am distclean-generic distclean-tags

dvi: dvi-recursive

dvi-am:

html: html-recursive

html-am:

info: info-recursive

info-am:

install-data-am: install-dist_applicationsDATA install-dist_extraDATA \
	install-dist_griloconfDATA install-dist_icon_actionsDATA \
	install-dist_icon_categoriesDATA install-dist_icon_statusDATA \
	install-dist_infobarstyleDATA install-dist_jsonDATA \
	install-dist_networksDATA install-dist_rebinderDATA \
	install-dist_shellstyleDATA install-dist_slideshowstyleDATA \
	install-dist_styleDATA install-dist_videocontrolsstyleDATA \
	install-dist_volumecontrolstyleDATA

install-dvi: install-dvi-recursive

install-dvi-am:

install-exec-am:

install-html: install-html-recursive

install-html-am:

install-info: install-info-recursive

install-info-am:

install-man:

install-pdf: install-pdf-recursive

install-pdf-am:

install-ps: install-ps-recursive

install-ps-am:

installcheck-am:

maintainer-clean: maintainer-clean-recursive
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-recursive

mostlyclean-am: mostlyclean-generic mostlyclean-libtool

pdf: pdf-recursive

pdf-am:

ps: ps-recursive

ps-am:

uninstall-am: uninstall-dist_applicationsDATA uninstall-dist_extraDATA \
	uninstall-dist_griloconfDATA uninstall-dist_icon_actionsDATA \
	uninstall-dist_icon_categoriesDATA \
	uninstall-dist_icon_statusDATA uninstall-dist_infobarstyleDATA \
	uninstall-dist_jsonDATA uninstall-dist_networksDATA \
	uninstall-dist_rebinderDATA uninstall-dist_shellstyleDATA \
	uninstall-dist_slideshowstyleDATA uninstall-dist_styleDATA \
	uninstall-dist_videocontrolsstyleDATA \
	uninstall-dist_volumecontrolstyleDATA

.MAKE: $(RECURSIVE_CLEAN_TARGETS) $(RECURSIVE_TARGETS) ctags-recursive \
	install-am install-strip tags-recursive

.PHONY: $(RECURSIVE_CLEAN_TARGETS) $(RECURSIVE_TARGETS) CTAGS GTAGS \
	all all-am check check-am clean clean-generic clean-libtool \
	ctags ctags-recursive distclean distclean-generic \
	distclean-libtool distclean-tags distdir dvi dvi-am html \
	html-am info info-am install install-am install-data \
	install-data-am install-dist_applicationsDATA \
	install-dist_extraDATA install-dist_griloconfDATA \
	install-dist_icon_actionsDATA install-dist_icon_categoriesDATA \
	install-dist_icon_statusDATA install-dist_infobarstyleDATA \
	install-dist_jsonDATA install-dist_networksDATA \
	install-dist_rebinderDATA install-dist_shellstyleDATA \
	install-dist_slideshowstyleDATA install-dist_styleDATA \
	install-dist_videocontrolsstyleDATA \
	install-dist_volumecontrolstyleDATA install-dvi install-dvi-am \
	install-exec install-exec-am install-html install-html-am \
	install-info install-info-am install-man install-pdf \
	install-pdf-am install-ps install-ps-am install-strip \
	installcheck installcheck-am installdirs installdirs-am \
	maintainer-clean maintainer-clean-generic mostlyclean \
	mostlyclean-generic mostlyclean-libtool pdf pdf-am ps ps-am \
	tags tags-recursive uninstall uninstall-am \
	uninstall-dist_applicationsDATA uninstall-dist_extraDATA \
	uninstall-dist_griloconfDATA uninstall-dist_icon_actionsDATA \
	uninstall-dist_icon_categoriesDATA \
	uninstall-dist_icon_statusDATA uninstall-dist_infobarstyleDATA \
	uninstall-dist_jsonDATA uninstall-dist_networksDATA \
	uninstall-dist_rebinderDATA uninstall-dist_shellstyleDATA \
	uninstall-dist_slideshowstyleDATA uninstall-dist_styleDATA \
	uninstall-dist_videocontrolsstyleDATA \
	uninstall-dist_volumecontrolstyleDATA


# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
